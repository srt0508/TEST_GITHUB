bteq << EOF

.LOGON 153.65.14.249/abhishekb,abhishekb;

.SET MAXERROR 1
.SET ERRORLEVEL 3807 SEVERITY 0; /* Ignore "Table does not exist" error  */

DROP TABLE DEVOPS.OPB_SUBJECT_DEVOPS_TEST;

CREATE MULTISET TABLE DEVOPS.OPB_SUBJECT_DEVOPS_TEST ,NO FALLBACK ,
     NO BEFORE JOURNAL,
     NO AFTER JOURNAL,
     CHECKSUM = DEFAULT,
     DEFAULT MERGEBLOCKRATIO
     (
      SUBJ_NAME VARCHAR(240) CHARACTER SET LATIN NOT CASESPECIFIC,
      SUBJ_ID DECIMAL(5,0),
      INUSE DECIMAL(5,0),
      SUBJ_CREATOR VARCHAR(240) CHARACTER SET LATIN NOT CASESPECIFIC,
      SUBJ_DESC VARCHAR(2000) CHARACTER SET LATIN NOT CASESPECIFIC,
      SRCTYPE DECIMAL(5,0),
      IS_SHARED DECIMAL(5,0),
      OWNER_ID DECIMAL(5,0),
      GROUP_ID DECIMAL(5,0),
      PERMISSIONS DECIMAL(5,0),
      CREATION_TIME VARCHAR(30) CHARACTER SET UNICODE NOT CASESPECIFIC,
      FOLDER_ATTR DECIMAL(5,0),
      SUBJ_GID VARCHAR(240) CHARACTER SET LATIN NOT CASESPECIFIC,
      CREATE_INFO DECIMAL(5,0),
      OBJVERSION DECIMAL(5,0),
      VERSION_STATUS DECIMAL(5,0),
      OS_USER VARCHAR(240) CHARACTER SET LATIN NOT CASESPECIFIC)
UNIQUE PRIMARY INDEX ( SUBJ_ID );

INSERT INTO DEVOPS.OPB_SUBJECT_DEVOPS_TEST
SELECT * FROM ETL_META.OPB_SUBJECT;

.LOGOFF;
.QUIT;

EOF